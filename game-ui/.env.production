# Part A is running, so builds point at the real API and the real generated art.
# Set VITE_USE_MOCKS=1 to produce a self-contained MSW demo build instead.
VITE_API_BASE_URL=http://localhost:3000/api
VITE_USE_MOCKS=0
