# game-api does not exist yet, so a production build demos on MSW too.
# Flip VITE_USE_MOCKS to 0 once Part A is running to point at the real API.
VITE_API_BASE_URL=http://localhost:3000/api
VITE_USE_MOCKS=1
