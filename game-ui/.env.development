# 0 = live game-api and real generated art. Requires the API up:
#   POSTGRES_PORT=5433 docker compose up -d postgres && npm run dev:api
# Set to 1 to fall back to the MSW mocks and work with the backend down.
VITE_API_BASE_URL=http://localhost:3000/api
VITE_USE_MOCKS=0
